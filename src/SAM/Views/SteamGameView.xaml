<UserControl x:Class="SAM.Views.SteamGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewModels="clr-namespace:SAM.ViewModels"
             xmlns:stats="clr-namespace:SAM.Views.Stats"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type viewModels:SteamGameViewModel}, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Wpf.Ui;component/controls/button/button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Wpf.Ui;component/controls/toggleswitch/toggleswitch.xaml" />
            </ResourceDictionary.MergedDictionaries>
  </ResourceDictionary>
    </UserControl.Resources>

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:CurrentWindowService />
    </dxmvvm:Interaction.Behaviors>

    <Grid>
        <DockPanel LastChildFill="True">
            <StatusBar DockPanel.Dock="Bottom" Height="70" Padding="2">
                <StatusBarItem Content="Show Hidden:" FontWeight="SemiBold" FontSize="13" Margin="5,0" />
                <StatusBarItem>
                    <ui:ToggleSwitch IsChecked="{Binding ShowHidden, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <ui:ToggleSwitch.Style>
                            <Style BasedOn="{StaticResource DefaultUiToggleSwitchStyle}" TargetType="{x:Type ui:ToggleSwitch}">
                                <Setter Property="ToolTip" Value="Hide Hidden Achievements" />
                                <Style.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter Property="ToolTip" Value="Show Hidden Achievements" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ui:ToggleSwitch.Style>
                    </ui:ToggleSwitch>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <ui:Button Command="{Binding CopyUserStatsCommand}" Icon="Copy20" Content="Copy Stats">
                            <ui:Button.Style>
                                <Style TargetType="{x:Type ui:Button}" BasedOn="{StaticResource DefaultUiButtonStyle}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Width" Value="120" />
                                    <Setter Property="Appearance" Value="Secondary" />
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="True">
                                            <Setter Property="Appearance" Value="Primary" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                        <ui:Button Command="{Binding UnlockAllAchievementsCommand}" Icon="LockOpen20" Content="Unlock All"
                                   IsEnabled="{Binding AllowUnlockAll, Mode=OneWay}">
                            <ui:Button.Style>
                                <Style TargetType="{x:Type ui:Button}" BasedOn="{StaticResource DefaultUiButtonStyle}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Width" Value="120" />
                                    <Setter Property="Appearance" Value="Secondary" />
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="True">
                                            <Setter Property="Appearance" Value="Primary" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                        <ui:Button Command="{Binding ResetCommand}" Icon="ArrowUndo20" Content="Reset"
                                   IsEnabled="{Binding IsModified, Mode=OneWay}">
                            <ui:Button.Style>
                                <Style TargetType="{x:Type ui:Button}" BasedOn="{StaticResource DefaultUiButtonStyle}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Width" Value="120" />
                                    <Setter Property="Appearance" Value="Secondary" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsModified}" Value="True">
                                            <Setter Property="Appearance" Value="Danger" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                        <ui:Button Command="{Binding SaveCommand}" Icon="Save20" Content="Save"
                                   IsEnabled="{Binding IsModified, Mode=OneWay}">
                            <ui:Button.Style>
                                <Style TargetType="{x:Type ui:Button}" BasedOn="{StaticResource DefaultUiButtonStyle}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Width" Value="120" />
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="True">
                                            <Setter Property="Appearance" Value="Primary" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </StackPanel>
                </StatusBarItem>
            </StatusBar>
            <Grid>
                <TabControl SelectedIndex="{Binding SelectedTabIndex}">
                    <TabItem Header="Achievements">
                        <stats:AchievementsView />
                    </TabItem>
                    <TabItem Header="Stats">
                        <stats:StatsView />
                    </TabItem>
                    <TabItem Header="Auto Unlock">
                        <Grid Margin="10">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Live Status Display -->
                            <StackPanel Grid.Row="0" Margin="5">
                                <TextBlock Text="{Binding CurrentUnlockStatus}" FontSize="16" FontWeight="Bold" />
                                <TextBlock Text="{Binding NextUnlockCountdown}" FontSize="14" Foreground="DarkSlateGray" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
                                <TextBlock Text="API Key:" VerticalAlignment="Center"/>
                                <TextBox Width="200" Text="{Binding WebAPIKey, UpdateSourceTrigger=PropertyChanged}" Margin="5,0"/>
                                <TextBlock Text="SteamID:" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                <TextBox Width="200" Text="{Binding SteamID, UpdateSourceTrigger=PropertyChanged}" Margin="5,0"/>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <Button Content="Start" Command="{Binding StartAutoUnlockCommand}" Width="80" Margin="5"/>
                                    <Button Content="Stop" Command="{Binding StopAutoUnlockCommand}" Width="80" Margin="5"/>
                                </StackPanel>
                            </StackPanel>
                            <!-- Separator -->
                            <Separator Grid.Row="1" Margin="0,10" />

                            <!-- Log Viewer -->
                            <GroupBox Grid.Row="3" Header="Live Log">
                                <ListBox ItemsSource="{Binding AutoUnlockLog}" Margin="5" />
                            </GroupBox>
                        </Grid>
                    </TabItem>
                </TabControl>
                <TextBox Margin="551,2,10,346" Text="{Binding SearchText, Delay=300, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_TextChanged" VerticalAlignment="Top"/>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>